# 《C++现代程序设计》课程项目任务书

## 一、项目开发组织形式

1. 项目开发必须以小组形式进行，每组2~4人。小组自由组合，推选出组长。
2. 要求小组成员分工明确，确定各自承担的任务量。
3. 成果由组长提交。

## 二、项目成果提交

1. 根据报告模板撰写项目报告。报告命名为：
   `2025-2026-2学期《现代C++程序设计》课程-项目报告-小组名或姓名.docx`

2. 根据成员表现，填写报告附1中的项目评分表中的学生自评表。

3. 将所有源代码（.cpp、.h/.hpp、数据文件、Makefile）放在同一个目录中。请勿包含可执行代码。

4. 运行环境配置信息写在一个文件里，命名为：`config.md`，存放在源代码目录中。内容包括操作系统、编译环境等。

5. 将报告、评分表和源代码目录打包在一个压缩包中，命名为：`小组名.zip`

6. 将`.zip`在`2026/06/21 23:59`前发送至：`baizj@uestc.edu.cn`，邮件主题为：`xxx小组的C++项目成果`

## 三、评价依据

根据项目完成度、代码质量和报告质量进行：

1. 未按要求做的，得0分
2. 过期提交的，得0分
3. 项目代码和报告只要缺一样，得0分
4. 项目代码明显是抄袭的，得0分
5. 报告格式不合格的，得0分

---

# 附件1：项目要求

## 一、内容和要求

运用C++知识群和其它知识，对标MySQL，开发一个微型关系型数据库管理系统。

要求采用团队开发形式，建议使用VibeCoding技术，编写项目的全部代码。要求自行编写单元测试代码，并通过测试。

## 二、系统功能要求

### （一）具体功能要求

#### 1. 存储功能

采用宿主操作系统的文件系统。数据表、索引（如果有）等用文件，目录结构和文件格式自定。

目录结构和文件命名方式示例：
```
data/                          // 目录
├── person/                    // 目录，库名
│   ├── person.dat             // 表文件
│   └── person.idx             // 同名表的索引文件
└── other/
```

#### 2. DDL

至少实现如下语句：

**1) create database**

- 功能：创建数据库
- 语法：`create database <dbname>`
- 注：尖括号`<>`标记括起了参数，使用时需给出具体名字，例如：`create database person`

**2) drop database**

- 功能：删除数据库
- 语法：`drop database <dbname>`

**3) use**

- 功能：切换数据库
- 语法：`use <dbname>`

**4) create table**

- 功能：创建表
- 语法：`create table <table-name> ( <column> <type> [primary], ...)`
- 表名和列名：全英文小写，不含`_`和特殊字符
- 数据类型type至少支持以下两种：
  - `int`：采用C++的默认长度，不考虑多种int
  - `string`：最长256字符的定长串，编码类型UTF-8
- 如指定列是primary（主键），则须为表建立索引，索引数据结构采用B+树

**5) drop table**

- 功能：删除表
- 语法：`drop table <table-name>`
- 注：删除表的同时，删除对应的索引（如果有）

> 请自行设计所有的错误处理。

#### 3. DML

至少实现如下语句：

**1) select**

- 功能：根据条件（如果有）查询表，显示查询结果
- 语法：`select <column> from <table-name> [ where <cond> ]`
- `<column>`：`<column-name>` | `*`（只需一个列名，`*`表示所有列）
- where子句：可选，如无表示无条件查询
- `<cond>`：`<column> <op> <const-value>`
- `<op>`：`=`、`<`、`>`三者之一
- 提示：如果表有索引，请使用它

**2) delete**

- 功能：根据条件（如果有）删除表中的记录
- 语法：`delete from <table-name> [where <cond>]`
- where子句：语法同select

**3) insert**

- 功能：在表中插入数据
- 语法：`insert into <table-name> values (<const-value> [, <const-value>...])`
- 注：字符串数据用双引号括起来

**4) update**

- 功能：根据条件（如果有）更新表中的记录。如无条件，则更新整张表
- 语法：`update <table-name> set <column> = <const-value> [where <cond>]`
- where子句：语法同select
- 注：字符串数据用双引号括起来

> 以上是对DDL、DML的最低要求。小组可以根据实际情况对上述语句进行扩展。

### （二）界面要求

模仿MySQL的交互式界面。例如：

```
> create table person (id int primary, name string)
[反馈信息在这里]

> use person
[反馈信息在这里]

> insert person values(1001, "peter")
[反馈信息在这里]

> select name from person where id = 1001
[反馈信息在这里]

> exit
```

反馈信息请自行设计，但必须清晰明了。

## 三、编码要求

### （一）关于程序结构

1. **架构必须采用C/S架构**，服务器和客户端分开

   - 服务器和客户端的通信协议建议采用TCP/IP
   - 用户使用客户端应用登录系统
   - 用户在CLI界面输入SQL语句，通过网络发送给服务端
   - 服务端解析语句并执行，得到结果集，然后通过网络将结果集发送回客户端
   - 客户端将结果集格式化（对标MySQL）后，显示在屏幕上
   - 服务器和客户端交换的数据格式自定（例如JSON），需编写序列化和反序列化代码

2. **建议代码至少包含如下类（模板）**，但可以根据设计自行设定：

| 序号 | 类（模板）名 | 说明 |
|------|--------------|------|
| 1 | column | 表中的列 |
| 2 | row | 表中的行，是column的容器 |
| 3 | table | 表，是row的容器。table的子集也是一张table，可以用于存储查询返回的结果集 |
| 4 | index | 表的索引，是一棵B+树 |
| 5 | database | 数据库 |

3. 其他相关类（模板），例如：SQL语句解析器、执行引擎、存储引擎、网络引擎、单元测试等，请自行设计。

### （二）关于代码编写和构建

1. 强烈建议使用VibeCoding技术编写代码（请自行搜索免费途径）
2. **不得使用C++的STL标准容器**，包括：array/deque/vector、list/forward_list、map/unordered_map、set/unordered_set、span、所有的容器适配器等。必须自己造轮子
3. 可以使用其他的基础组件、多线程库等，可以使用第三方网络库
4. C++标准至少到C++20，尽量到C++23
5. **必须在Linux系统下开发**（因为老师会在Linux系统下评估代码）
6. **必须使用CMake来构建应用**
7. 强烈建议使用Git进行代码托管（推荐使用国产Gitee）

### （三）关于报告撰写

允许报告用AI合成，但必须按照模板来。详见项目报告模板。